# redirect-path-chrome-extension
Redirect Path Extension flags HTTP status codes, client-side redirects, and response headers from the browser toolbar. Redirect Path Chrome Extension traces server hops, meta refreshes, and JavaScript redirects, including 301, 302, 404, and 500 results plus server type, cache headers, and server IP.
